@page
@model StrattonOakmont.Pages.Security.TypeOfSecurityModel

@{
    ViewData["Title"] = "Type Of Security";
}

<div class="card">
    <h2 style="text-align:center; margin:10px">Select the type of security for @Model.Company.Name</h2><br />

    <div class="card-body">
        <form class="form-horizontal form-material mx-2" method="post">
            <input asp-for="@Model.Company.Id" value="@Model.Company.Id" style="display:none;" />
            <div class="form-group">
                <label class="col-sm-12">Select category</label>
                <div class="col-sm-12 border-bottom">
                    @Html.DropDownList("Id", new SelectList(Model.Categories, "Id", "CategoryName"), "Select category")
                </div>
           </div>

            <div class="form-group">
                <div class="col-sm-12 d-flex">
                    <button type="submit" class="btn btn-success mx-auto mx-md-0 text-white">
                        Select
                    </button>
                </div>
            </div>
        </form>
    </div>
</div>
